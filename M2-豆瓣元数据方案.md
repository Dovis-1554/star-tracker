# M2 · 豆瓣元数据接入 实施方案

> 状态：**待确认**　日期：2026-09-17　前置：M1 已提交（`9d7eae2`），已推私有仓库 star-tracker
>
> 已并入乐生的三点修改：① 多次添加（第一季、第二季）要合并成一部剧 → 新增第 6 节「合并与去重」；
> ② 详情页新增一季也要接豆瓣 → 搜索页增加第二条入口（第 5 节）；
> ③ 搜索结果支持**多选**，一次勾选多季批量添加（第 5 节 + 第 6.2 节 + 验收清单）；
> ④（2026-09-19）**取消季名归一**（名字保留豆瓣原样，合并靠聚合识别）+ 排序规则（网络按上映时间、
> 本地按添加时间、自助排序菜单）——见第 6.1 节与第 8 节。

## 0. 一句话

在「新建条目」前插入一步豆瓣搜索：输入剧名 → 候选列表 → 选中 → 带预填进编辑页 → 保存时把豆瓣的**集数 / 年份 / 封面 / 豆瓣ID** 写库。搜不到或断网一律降级为手动录入。

---

## 1. 接口实测（2026-09-17 00:20 广州，本机网络，已复测通过）

```
GET https://movie.douban.com/j/subject_suggest?q=仙逆
Headers: User-Agent: <真实浏览器 UA>
         Referer: https://movie.douban.com/
→ 200，约 0.3s，返回 JSON 数组
```

返回字段实测：

| 字段 | 样例 | 用途 |
|---|---|---|
| `id` | `35679839` | → `externalId`，source=douban |
| `title` | `仙逆 第一季` | → 名称 |
| `year` | `2023` | → `releaseDate` |
| `episode` | `24` | → `totalEpisodes`（**最值钱的字段**） |
| `img` | `https://img3.doubanio.com/...jpg` | → 封面 |
| `type` | 恒为 `movie` | **不可用**，动漫/剧集区分不出来 |

实测命中情况：

| 关键词 | 结果 |
|---|---|
| 仙逆 | 第一季 24 集 / 年番2 52 集 / 年番3 52 集 ✅ |
| 漫长的季节 | 12 集 ✅ |
| 龙王、逆天邪神（架构文档历史实测） | 0 条 ❌ |

封面图：`img*.doubanio.com` **带 Referer 返回 200，不带返回 418**（已复测）。

**接口不提供**：评分、简介、类型区分、季结构。评分和简介 M2 不解决，`overview` 继续留空。

---

## 2. 接入方式（已定）

- 只用 `subject_suggest`，**不做详情页抓取**（架构文档已验证详情页返回 2.9KB 验证页，抓不到）
- 新增依赖 `http`（不用 `dio`——M2 只需要两个 GET，别引入重量级）
- 封面**下载到本地**（带 Referer），存 `coverPath`，同时存 `coverUrl` 备查。好处：复用现有本地封面机制、离线可见、绕开运行时 418，也不给首页/详情页引入网络图片组件
- 超时 4s；失败只提示，绝不阻塞手动录入

---

## 3. 数据模型：**不改表，无迁移**

现有 `Titles` 表的预留字段刚好够用：

| 现有字段 | 来源 |
|---|---|
| `externalId` | 豆瓣 subject id |
| `source` | `Sources.douban`（常量已存在） |
| `releaseDate` | `year` |
| `totalEpisodes` | `episode` |
| `coverUrl` | `img` |
| `coverPath` | 下载缓存的本地文件 |
| `score` / `overview` | 留空（源不给） |

→ `schemaVersion` 保持 1，**不需要写迁移**。也不新增缓存表（M2 只在录入时取一次，缓存是过度设计）。

Repository 需要补的：`create()` 目前不接受 `releaseDate`，加上；`saveEdit()` 不写 `externalId`/`coverUrl`，所以**编辑已有豆瓣条目不会把来源信息冲掉**（但要验证，见第 6 节）。

---

## 4. 代码结构

```
新增
lib/data/metadata/
  candidate.dart            // 候选：externalId/title/year/episode/coverUrl
  metadata_provider.dart    // abstract MetadataProvider { id; search(q) }
  douban_provider.dart      // 豆瓣 suggest 实现 + 封面下载
lib/features/search/
  search_title_page.dart    // 搜索页：输入/防抖/加载/结果/空/失败/手动入口

修改
lib/features/edit/edit_title_page.dart   // TitlePrefill 扩展字段
lib/data/repository/title_repository.dart // create 补 releaseDate 等
lib/features/list/title_list_page.dart    // ＋ 按钮改跳搜索页
```

按架构文档的 `MetadataProvider` 抽象写，但 **M2 只实现豆瓣一个源**（`tvmaze` 不实现——YAGNI，需要时再加）。

---

## 5. 交互流程

**两条入口**（乐生追加：详情页也要接豆瓣）

- A. 首页 `+` → 进**搜索页**（不再直接进空编辑页）→ 选中 → 编辑页 → 保存
- B. 详情页季标签行尾 `＋`（新增一季）→ 同样进**搜索页**，关键词自动预填为**基准剧名**，选中后作为该剧的**新一季**入库

共同步骤：

1. 输入关键词 → 防抖 350ms 自动搜索（也可手动触发）
2. 结果卡片列表：封面 + 标题 + 年份 + 集数 + **右侧勾选框**
3. **单选**：直接点卡片 → 带预填进编辑页（名称/总集数/年份已填，类型按 `episode` 猜：`1` → 电影，`>1` → 剧集，用户可改）→ 保存
4. **多选**（乐生追加 ③）：勾选多条 → 底部浮出「添加 N 项」→ 一次性创建 N 季
   - 批量创建**不逐条进编辑页**：类型按 `episode` 猜、状态默认「在看」、平台留空，建完可在编辑页改
   - 同一批里季名统一归一化，全部归属同一部剧（首页一张卡）
   - 已存在的季自动跳过，结束给汇总提示：「新增 2 项，跳过 1 项（已存在）」
   - 封面串行下载，单张失败该条降级为程序化占位图，不影响其余
5. 搜索页底部常驻「**手动创建**」→ 跳过搜索直接进空编辑页
6. 无结果 / 请求失败 → 提示「豆瓣没找到，可手动创建」+ 同样的手动入口

---

## 6. 合并与去重（乐生追加需求 ①）

诉求：**先添加第一季、再添加第二季，两季要合并成一部剧**（首页一张卡、详情页两个季标签）。
M1 的 `baseNameOf` 聚合已经具备这个能力，但前提是**名字必须归一成「第N季」**，而豆瓣给的是「第一季 / 年番2」这类写法——不归一化就会变成两张卡。

### 6.1 季号识别规则（**只识别，不改写名字**）

> 2026-09-19 修订：乐生要求**取消季名归一**——不再把「仙逆 年番2」改写成「仙逆 第2季」，
> 入库名保留豆瓣原始标题。合并改由 `TitleNames.baseNameOf / seasonNoOf` 在**聚合时识别**完成，
> 效果一样（首页一张卡、详情页两个季标签），但名字是来源原样。

`TitleNames`（core/constants.dart）同时认得这些季号写法：

| 写法 | 识别出基准名 | 季号 |
|---|---|---|
| `仙逆 第2季` | 仙逆 | 2 |
| `仙逆 第一季` | 仙逆 | 1 |
| `仙逆 第十一季` | 仙逆 | 11 |
| `仙逆 年番2` / `年番3` | 仙逆 | 2 / 3 |
| `Perfect World Season 2` / `S2` | Perfect World | 2 |
| `仙逆`（无季号） | 仙逆 | 1 |

详情页季标签显示规则不变：`第${seasonNoOf(name)}季`，所以「年番2」在标签上显示成「第2季」，但条目名保持原样。

### 6.2 合并

- 判定同剧：`baseNameOf` 相同即同一部剧
- 首页 `_groupSeasons` 已按基准名聚合 → 归一化后两季天然合成一张卡
- 首页卡片代表季 = 最后一季；详情页默认停在最后一季（M1 既有规则，无需改动）
- **多选批量添加时**：整批先归一化，再整批查重，一次性写入；同一批内若出现重复季号（豆瓣偶尔同季多个版本），只保留第一条

### 6.3 去重（防手滑重复建同一季）

保存前检查：若「同基准名 + 同季号」的条目已存在 → **不新建**，弹提示「第N季已存在」，并直接跳到该季的详情页。
避免搜索结果里点了两次「第一季」产生两条重复记录（M2 之前手工录入没有这个问题，接入搜索后误触概率变高）。

---

## 7. 与既有逻辑的衔接（三个必须处理的冲突点）

**① 季名归一化 —— 不处理的话多季聚合会失效**

豆瓣返回的是「仙逆 第一季」「仙逆 年番2」「仙逆 年番3」，而我们的 `baseNameOf` 只认「第N季」后缀。

→ 入库前做归一化：`第一季 → 第1季`、`年番N → 第N季`、`第二季/第三季 → 第N季`。
不归一化的话，「仙逆 第一季」和「仙逆 年番2」在首页会变成两张卡，与 M1 定下的「同剧一张卡」相悖。

**② 豆瓣封面不能被程序化封面顶掉**

`saveEdit` 现在的规则是「没有远程封面就每次保存重绘」。豆瓣条目 `coverUrl` 非空 → 走 `hasRemoteCover` 分支 → **不会**被重绘覆盖。这条要真机验证一次：编辑豆瓣条目改名后，封面应仍是豆瓣海报。

**③ 多季封面传播要跳过有远程封面的季**

`_syncCoverToSiblings` 已跳过 `coverUrl` 非空的季，豆瓣季不会被本地占位图冲掉。但反向：**没有豆瓣封面的季**会被传播成豆瓣封面——这是符合预期的（同剧共封面）。

---

## 8. 排序（乐生追加 ④）

| 场景 | 规则 |
|---|---|
| 搜索结果（网络） | 按**上映时间升序**（早年在前），无年份的排最后 |
| **首页** | **按打开（最后修改）时间倒序**，不提供排序菜单 |
| **详情页多季列表** | 默认按**上映年份升序**（第 1 季在前），季标签行排序按钮可切换：上映年份 / 季号 / 添加时间，选择存 `detail.seasonSort` |

首页的排序键取「整部剧」层面的最后修改时间最大值，时间相同时按剧名稳定兜底，避免刷新后顺序乱跳。
详情页默认停留的「最后一季」按**季号最大**计算，不取列表末尾（列表顺序会随排序方式变化）。

## 9. 防风控

- 固定 UA + Referer；搜索防抖；不并发、不批量；自用频率个位数/天
- 失败分类提示：网络超时 / 403·418（风控）/ 空结果，文案不同
- 接口一旦失效：搜索页直接可用手动模式，App 其余功能不受影响（Provider 层留开关位，M2 不做设置页）

---

## 10. 真机验收清单

**基础链路**
- [ ] 搜「仙逆」→ ≥3 条，集数分别为 24 / 52 / 52
- [ ] 选中「仙逆 第一季」→ 编辑页预填：名称 仙逆 **第1季**、24 集、2023、豆瓣封面
- [ ] 保存 → 首页出现卡片，封面是**豆瓣海报**不是占位图
- [ ] 进详情页 → 24 个集数格子

**合并（乐生诉求 ①）**
- [ ] 首页再 `+` → 搜「仙逆」→ 选「年番2」→ 保存后首页**仍只有一张卡**
- [ ] 进详情页 → 两个季标签（第1季 / 第2季），默认停在第 2 季，52 个格子
- [ ] 重复选同一季 → 提示「第2季已存在」，不产生重复记录

**多选（乐生诉求 ③）**
- [ ] 搜「仙逆」→ 勾选第一季 + 年番2 + 年番3 → 底部显示「添加 3 项」
- [ ] 点添加 → 一次建成三季，首页**仍只有一张卡**，详情页三个季标签
- [ ] 再次添加其中已存在的一季 → 提示「新增 0 项，跳过 1 项（已存在）」，不产生重复
- [ ] 点卡片（不勾选）→ 仍走单条编辑页流程

**详情页入口（乐生诉求 ②）**
- [ ] 详情页点 `＋` → 进搜索页且关键词已预填为「仙逆」
- [ ] 选中「年番3」→ 保存 → 详情页变成三个季标签，首页仍一张卡

**健壮性**
- [ ] 飞行模式 → 搜索失败有提示，「手动创建」仍可用，全流程不卡死
- [ ] 编辑豆瓣条目改名后保存 → 封面仍是豆瓣海报

---

## 11. 明确不做（边界）

- 不做评分 / 简介（源不给）
- 不做多源（TVmaze / TMDB 等留到真需要时）
- 不做设置页和开关 UI
- 不改数据库 schema、不写迁移
- 不做搜索结果缓存

---

## 12. 执行顺序

1. 加 `http` 依赖 + `pub get`
2. metadata 层：候选类 + Provider 抽象 + 豆瓣实现 + 封面下载
3. `normalizeSeasonName()` 季名归一化（含中文数字 / 年番N）+ 保存前同季去重检查
4. 搜索页 UI：防抖、五种状态（输入中/加载/结果/空/失败）、手动入口、**多选批量添加**、接收可选 `baseName` 用于详情页入口预填关键词
5. 详情页 `＋` 改为跳搜索页（带基准剧名）
6. 编辑页 `TitlePrefill` 扩展 + repository 补 `releaseDate`
7. `analyze` → 装机 → 按第 9 节清单逐条真机验收

新增 4 个文件、修改 4 个文件（比原方案多改 `title_detail_page.dart`）。
